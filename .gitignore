# 本仓用于承载操作系统模板本体。项目实例可放在 项目区/本地实例/，
# 但不跟随模板仓库提交，避免把业务代码、产物或密钥带进 czxt。

项目区/本地实例/*
!项目区/本地实例/.gitkeep

# 模板仓库只保留项目卡模板；具体项目卡属于本地实例或明确审批后的样例。
项目配置/*.project.json
!项目配置/_模板.project.json

*.log
*.tmp
.DS_Store
Thumbs.db
