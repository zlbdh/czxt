# 四项回归修复实施计划

**目标**：修复 2026-09-04 已复现的四项缺陷，补齐行为回归并按 ADR-016 提交推送。
**授权**：zlbdh 明确要求“继续修复这 4 项，补齐回归后再提交推送”。
**依据**：本任务上一轮只读审查与 Windows PowerShell 5.1 实测；起点 `4639f6abe9813ccd5c0e7c38d23f60374bfacf24`，本地/远端一致、工作区 clean。
**架构**：沿用现有模板/实例与受管文件事务，不重构 capture 核心；补参数渲染、Git 改动识别及 ADR 真值计算三个独立边界，治理收档由主会话单点落笔。
**技术栈**：Windows PowerShell 5.1、Parser AST、JSON 序列化、Git porcelain 状态、现有契约测试工具。

## 全局约束

- 保留 UTF-8 BOM + LF 的 PS1 兼容基线；不引入依赖。
- 参数是数据，不能变成脚本语句、正则或路径外写；支持双引号项目名与项目根内多层业务目录。
- 维持既有 no-overwrite/CAS/ownership/Force 保留语义；来源和事项内容不被模板替换。
- 测试临时路径唯一且仅清理自身；实例化套件与会在模板中短暂写 fixture 的 hooks 套件串行。
- 不改全局配置，不删除业务实例或历史失败现场；不 force/rebase/tag/release。
- Git 提交推送由主会话单点执行；push 失败停止且不重试；最终核验远端 hash 与 clean 状态。

## 任务与验收

### 1. 实例化参数按上下文渲染

- 写集：`实例化项目.ps1`、`installer-*.ps1` 的渲染/输出模块、依赖这些参数的 hooks/P4b 消费处及相应测试；不改 PM/ADR 守卫。
- 先增加双引号名称、多层业务路径的实际渲染/运行回归，证明现有实现失败。
- JSON 对值序列化；PowerShell 区分字符串/注释/文档内容，禁止参数作为可执行表达式；路径显示、正则与临时文件名分开处理。
- 在写成功 marker 之前验证受管 JSON/PS1，验证失败不得输出成功。
- 正例覆盖普通中文名、双引号/单引号/美元符号名称、嵌套路径；负例保留路径越界拒绝。
- [x] RED：初始实例化 2/14；Regex 1/7；锚点 0/8；短名 8/9
- [x] GREEN：渲染 15/15、锚点 8/8、Regex/P4b 10/10、实际实例化 17/17、双 Stop 2/2
- [x] 独立复审：根数组、嵌套表达式、变量边界、单次替换、空 JSON 与花括号路径回归补齐；无遗留 P0-P2

### 2. PM 轨迹改动检测

- 写集：`check-pm-tracking.ps1`、专用 helper 及专用合同。
- 独立 Git 根用 Git 工作区/暂存区状态，不能把已经提交文件的 mtime 当作未留痕改动；非 Git 项目保留 mtime 回退。
- 测试 clean Git + 旧轨迹、新增/修改/暂存/删除/改名 framework、仅业务或忽略文件、非 Git、嵌在外层仓库内以及 Git 读取失败。
- 不通过更新状态时间戳消除 RED；原脚本出口 0/5/10 与 P4f 包装兼容。
- [x] RED：旧实现 13 项中 6 失败，覆盖 clean/业务/忽略/嵌套根/Git 失败错判
- [x] GREEN：25/25，覆盖 CP936 中文路径、两个模式 marker、direct 0/5/10 与 Git 失败；测试使用固定 `business/` fixture，不随实例参数改写
- [x] 独立复审：原 P1/P2 已解决，无新 P0-P2；纠正首轮把汇总行计入合同数的 26→25 口径

### 3. ADR 计数真源

- 写集：`p4i-governance-count-anchor.ps1`、`p4s-template-cleanliness.ps1`、专用 helper/合同；入口最后由主会话同步。
- 从真实 ADR 文件和索引状态单元格计算总数、现行和被替代/覆盖数量；校验文件与索引集合一致。
- 删除强制旧 38/35 文案的守卫；新增 ADR、状态变化及入口失配必须能改变门禁结果。
- [x] RED：旧实现专用合同 0/10；旧入口与集合/状态异常均未被完整拦截
- [x] GREEN：专用合同 12/12（原 11 + 清理隔离 1）；真实 P4i/P4s 识别 39/36/3 且 exit 0
- [x] 独立复审：状态变化与 P4s 负例补齐后通过，无 P0-P2

### 4. PROP-003 收档与最终发布

- 主会话核对 `7bf9ce4` 已含 PROP-003 范围与授权记录，再移至已完成并更新所有活跃引用/索引/当前状态；保留历史语义。
- 完成前冻结代码，串行跑实例化与 hooks；重跑模式/编码/Gate 0、文档、capture、P4t、总健康、README/交接检查与 diff。
- 独立审查最终改动，明确 P3/历史提醒，不把这些称为零风险。
- 分批提交可审查的代码与收档记录，正常 push 并核验本地/远端一致。
- [x] PROP 已完成且无活跃断链（原状态验证器 exit 0；计数 1/0/3/0/0；旧活跃路径零命中）
- [x] 新增回归与现有完整门禁通过
- [x] 最终独立复审通过：唯一新增 P2 清理范围已修复并独立实测，无遗留 P0-P2
- [x] 提交、推送、远端核验：修复提交 `df43c157493073e90fe7cf8470558c87b660b368` 正常推送成功，远端同 hash，main 分叉 0/0、工作区 clean；本条属于随后单独提交的文档回执

## 执行记录

- 2026-09-04 16:07：起点仍 clean，前序 RED 已只读复现；本轮先补永久回归，再修实现。
- 2026-09-04 16:39：直接以 `-Root .` 调单个 `prop-status-anchor.ps1` 触发已知相对根误用；改传 `Resolve-Path` 后 exit 0。正式入口会先绝对化 Root，本项不是新产品缺陷。
- 2026-09-04 17:00：首轮完整 scaffold 为 56 通过 / 7 失败；失败集中在测试最小模板漏复制新 renderer，以及 renderer 自测资产被错误实例化导致字节漂移。已纳入任务 1 RED，不重复原命令，修后冻结重跑。
- 2026-09-04：修复后完整 scaffold 63/63、capture 8/8、P4t 10/10、模式 3/3、编码合同 10/10、Gate 0 12/12、文档 148/148、编码检查 375 个脚本通过。三个专项独立复审通过；hooks 最后一轮未取得完成回执，不计为通过。
- 2026-09-21 19:24：恢复收尾；main 与远端仍为 `4639f6a`，全部修复尚未提交。补跑失效进程对应的 hooks，并复验 PM/ADR、新实例与最终体检；旧日期测试只按历史证据列示。
- 2026-09-21：当前环境参数 52 项、PM 25 项、hooks-smoke、文档 148 项、README/索引及 P4a-P4t 已通过。终审额外发现 ADR 合同清理共享临时父目录的 P2；隔离沙盒 RED 0/1 后，改为只清本轮登记的 fixture 并校验绝对父目录与非链接属性，GREEN 12/12，独立清理合同复验 1/1，最终复审通过。
- 责任隔离：任务 1/2/3 不共享生产写文件，公共清单、状态/交接/CHANGELOG 由主会话合并。
- 验证结论和修复取舍写入最终交接卡；历史配置恢复/原有大文件债不在本轮扩展范围。
