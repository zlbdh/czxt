# RETRO-001 · 2026-05 复盘

- **触发**：完成第 1 批 L3+ 改动（规范增量补齐 6 项 + F-205 ErrorBoundary L3 + 代码迁移到 {{APP_REPO_DIR}}/ L4）
- **日期**：2026-05-08
- **参与**：zlbdh + 咪咪
- **窗口**：2026-05-08 单次会话内

---

## 1. 这批改动做了什么

| 类别 | 内容 | 输出 |
|---|---|---|
| 规范增量补齐 | L1-L4 改动分级 / AI 边界 / DoD 检查清单 / ADR 体系 / RETRO 体系 / 确认改动池 | 6 项规范文档 |
| L3 代码 | F-205 ErrorBoundary（class component + 5 个 vitest 测试 + 5 个 Route 包裹 + Profile 页"最近的小问题"段） | 4 文件、5 测试 |
| L4 重构 | 代码 + 配置迁到 `{{APP_REPO_DIR}}/` 子目录，根目录从 22 项 → 7 项 | 14 项 mv + 9 个文档路径同步 |
| ADR | ADR-001（HashRouter 追溯）+ ADR-002（{{APP_REPO_DIR}}/ 迁移） | 2 篇决策记录 |
| 技术拆解 | sprint-1-技术拆解.md F-205 段 | 1 篇 |
| 提议 | PROP-001-拆分大文件 | 1 篇 |
| 修复 | mount 截断事故修复 Sprint-1需求清单 + 需求历史 | 2 文件恢复 |

**全程**：约 3-4 小时单次会话。

---

## 2. 哪里顺（值得保留的做法）

- ⭐ **审计先于动手**：发现 zlbdh 已有 `rules/`+`Docs/` 框架后，及时从 B 路径（推倒重建）切到 C 路径（增量补齐），避免破坏已有工作
- ⭐ **L4 用 mv 不用 cp/rm**：保留时间戳和 inode，sub-second 完成；node_modules 也无需重装
- ⭐ **AI 提议先入 `确认改动/待审批/`**：PROP-001 拆分大文件走正确流程，没污染 PRD
- ⭐ **静态括号 + import 解析替代 esbuild**：沙箱跑不了 esbuild Linux binary 时的有效 fallback
- ⭐ **每个 L3+ 改动同步更新 ADR/CHANGELOG/技术拆解**：DoD 清单防止漏文档
- ⭐ **遇到不可逆操作主动暂停问 zlbdh**：删空目录、移代码 这些都先 ask
- ⭐ **mount 截断事故 + Python 兜底**：事故发生后立刻用 Python errors='replace' 容错读，确认损失范围，再用 Python heredoc 重写恢复

---

## 3. 哪里卡（坑 / 低效）

| 痛点 | 详情 | 严重程度 |
|---|---|---|
| 🚨 **Edit 报告 success 但实际损坏** | `Sprint-1需求清单.md` Edit 后只剩 8250 字节、`需求历史.md` 丢了 16 行（v1.x + 决策记录段）。Edit 工具的成功消息**完全不可信** | 严重 |
| 🚨 **沙箱 vitest 跑不了** | rolldown 缺 Linux binary（Windows node_modules 拷过来不能用）。DoD 里的"vitest 全过"只能手动让 zlbdh 在 Windows 跑 | 中 |
| ⚠️ **rm 在 mount 受限** | 删除空目录需要 `cowork__allow_cowork_file_delete` 二次授权，3 种 shell 方法都被拒后才发现工具 | 低（一次性） |
| ⚠️ **沟通"什么意思？"** | 提了 3 个选项给 zlbdh 选，但用文字而不是视觉化目录树对比。zlbdh 看不明白只回了"什么意思"。后来用对比目录树秒懂 | 低 |
| ⚠️ **审计前推荐方案不充分** | 一开始建议 B 路径（推倒重建），没问 zlbdh "你之前有没有现成结构"。审计后才发现已有 rules/+Docs/ 完整框架，差点白做 | 低 |
| ⚠️ **L4 迁移没自动检测 build-apk.bat 内部路径** | 实际上 build-apk.bat 用 `cd /d "%~dp0"`，所以无需改路径。但我先入为主以为要改，差点白改 | 低（自查发现） |

---

## 4. 要不要改规范？

| 想改的 | 改到哪 | 优先级 | 是否提 PROPOSAL |
|---|---|---|---|
| Edit 工具对 >6KB 文件必须用 Python | 已落实在 `rules/code/已知技术约束.md` ★ 2026-05-08 段 | P0 | ✅ 已做 |
| 拆分 Home.jsx + components.jsx 等 5 个超 9KB 源文件 | PROP-001 已写 | P1 | ✅ 已写 PROP-001（待 zlbdh 审批） |
| 沙箱 vitest 受限的现实写进 `rules/operation/跑测试.md` | 跑测试.md 加一段"rolldown Linux binary 缺失场景，转 Windows 路线" | P2 | 暂不（Sprint 1 余 7 需求做完再说） |
| L3+ 改动开工前先用视觉化目录树跟 zlbdh 沟通 | `rules/ai/PM-产品经理.md` 加 1 条"分两个/多个方案时优先画目录树而不是文字列表" | P2 | 暂不（下次 RETRO 再判断要不要写） |
| 审计先于推荐：开干前主动查项目已有目录 | 已隐含在 PM playbook 步骤 1，但可以加更显性"审计 → 再推荐方案"的子步骤 | P3 | 暂不 |
| 任何 Edit 操作后强制 wc -c + xxd tail 验证 | 已隐含在 Dev playbook，但事故说明要做成"刚性"。可以写一个 `verify-write.sh` 脚本封装 | P2 | 暂不 |

---

## 5. 上次复盘提的事，这次跑得怎样？

第一次 RETRO，无上次。后续 RETRO 务必填这一节。

---

## 6. 一句话总结

> **规范 + L3 代码 + L4 重构 + 修事故，3-4 小时单次会话推到位。**
>
> **最大教训**：Edit 工具不可信赖，>6KB 必须 Python；规范要先写检查动作再写检查内容。
>
> **最大收获**：分层结构（`{{APP_REPO_DIR}}/` + `rules/` + `Docs/` + `确认改动/`）真的让根目录从 22 项收敛到 7 项，下次找东西不会再翻——zlbdh 的"为什么这么乱"反向触发了 L4，是这次会话最高 ROI 的一句话。

---

**附**：本次会话产出的 ADR / PROPOSAL / 技术拆解 / 文档可作为后续工作的"基线"。下次 RETRO（RETRO-002）触发条件 = 完成 Sprint 1 余 7 个 L3 需求后。
