---
name: retro-020
scope: project
type: episodic
loaded: on-demand
description: RETRO-020 — v3.39-v3.42 四闭环：便当食品安全 + 首破 schema-less 连胜（2 把 schema 数据刀）+ schema 迁移安全定式确立 + 数据刀隐私维度
---

# RETRO-020（2026-06-08 ~ 06-09）

**触发批次**：v3.39.0 C 便当模式（ultracode 食品安全）/ v3.40.0 C 出行备货（确定性收尾）/ v3.41.0 E 重量曲线（🏆 首破 schema-less ~28 刀）/ v3.42.0 M 偏离记录（第 2 把 schema + 隐私）。
**数字**：vitest 1549→1623 全绿；prod v3.38→v3.42；IndexedDB v140→v160（两次安全升级，零丢失实证）；本 session 累计 26 刀。

## 一、这批做了什么

- v3.39 便当（C-F10）：lunchbox.js 注入 mealRecommend，ultracode 食品安全审抓 3 真隐患（米饭蜡样芽孢杆菌/及时冷藏/复热一次）。
- v3.40 出行备货（C）：event-triggered 建议区仿低库存一键加，0 schema/0 LLM 确定性收尾 C 小刀 backlog。
- v3.41 重量曲线（E-F6）：**首破 schema-less 连胜**，Dexie v15 weightLogs 新空表 + recharts 折线 + 真机 v140→v150 零丢失。
- v3.42 偏离记录（M-F4 记录层）：第 2 把 schema v16 deviationLogs + chat 写库（静默 catch）+ 隐私（截断/可删除/不批判）；学习层留后续。

## 二、哪里顺（保留）

1. **🏆 schema 迁移安全定式确立**（2 把刀验证）：`V_N_STORES = {...V_{N-1}_STORES, newTable}` + `db.version(N)` **无 .upgrade()** + exportAllData 加表 + loadSnapshot 容错 + initial 兜底 + **真机「旧版造数→覆盖升级→逐项核老表」smoke**。两次（v15/v16）真机零丢失，schema-less 连胜可安全有意打破。
2. **数据刀 ultracode 隐私维度**（M）：敏感行为数据加隐私 lens——截断最小化（code-point 防 emoji 切坏）/ 仅本地 / 可删除（用户控制权）/ 不批判语气。隐私 lens 抓的「每条可删除」是真价值（C 类用户数据红线相邻）。
3. **投资刀确定性切法**（M）：把 multi-cut 的确定性部分先切——M 第一刀「只记录不学习」（detectionResult 现成写库 0 额外 LLM），LLM 学习层留后续。避免刚做完 schema 重刀马上接 LLM saga（RETRO-019 节奏管理兑现）。
4. **ultracode 食品安全持续兑现**（便当）：米饭蜡样芽孢杆菌（炒饭综合征）复热去不掉毒素=真伤人隐患，ship 前修。
5. **字节闸前置还债第 3-4 例**（week→Home banner、M→chatSuggestionSelect 抽块）：定式稳定，实施者自动执行。
6. **确定性刀连续一轮过**（出行/重量曲线/便当含审查/M 均 1 轮）；守发布铁律 0 失误。

## 三、哪里卡（教训/观察）

1. **schema bump 的隐藏改面是 snapshot 双向**：新表本身安全，但 exportAllData 漏加表=备份丢数据（C 类红线相邻）。两把刀都靠 AC + ultracode 双锁住，但这是「新表必带的 4 处改」（schema/exportAllData/loadSnapshot/initial）应固化为 checklist。
2. **stale-mount wc 持续**：每刀 wc 读旧字节值，正向 grep 命中 + git M 才可信。已稳定用「正向命中」口径复核，非阻塞但持续消耗。
3. **黄区文件累积**（useAppData 8184/snapshot 6798/useChatController 抽后 7850）：数据刀反复碰这几个枢纽文件,字节逼近。下次碰需前置抽。
4. **小刀直通 backlog 见底**（本批确认）：A/D/F/G/H/I/K 清零 + B/C/E/M 主刀 ship，剩学习层/daysLeft/节假日/J(P2)=投资级。项目进入「主体完成」阶段。

## 四、要不要改规范（action items）

| 项 | 去向 |
|---|---|
| 🏆 schema 迁移安全定式正式化（新表 4 处改 checklist + 无 upgrade + 真机零丢失 smoke）→ ADR 候选 ⭐⭐⭐⭐ | TASKS 候选池 |
| 数据刀 ultracode 隐私 lens（敏感数据：截断/本地/可删除/不批判）⭐⭐⭐⭐ | TASKS 候选池 |
| 投资刀确定性切法（multi-cut 先切确定性部分、LLM 留后续）⭐⭐⭐ | RETRO 观察项 |
| 黄区枢纽文件（useAppData/snapshot/useChatController）下次碰前置抽 | 监控 |
| 沉淀 PM 正式化批次（RETRO-016~020 候选合并 → ADR/元规则）⭐ 累积已厚 | 待 zlbdh 触发 |
EOF
echo "RETRO-020 done"