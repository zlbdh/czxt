# RETRO-XXX · YYYY-MM 复盘

- **触发**：完成了第 N 批 L3+ 改动（如 F-001 / F-006 / F-002）
- **日期**：YYYY-MM-DD
- **参与**：zlbdh + 咪咪

---

## 1. 这批改动做了什么

按 PRD 对照写，简明：

| ID | 标题 | 计划工作量 | 实际工作量 | 是否上线 |
|---|---|---|---|---|
| F-XXX | xxx | M | M | ✅ |
| F-YYY | yyy | S | L（卡了一天） | ✅ |

**输出物**：vX.Y dev APK / N 个 ADR / N 篇文档更新 / ……

---

## 2. 哪里顺（值得保留的做法）

写 3-5 条具体的事，不要写"沟通顺畅"这种废话。

例：
- 「先在 sandbox 跑通再 cp 到 src，避免了一次 mount 截断」
- 「zlbdh 在 PRD 上加 ASCII 草图，咪咪一遍写对，不用返工」
- 「每改一个文件 wc -lc + tail -3 验证，没出现过文件截断」

---

## 3. 哪里卡（坑 / 低效 / 没必要的步骤）

写 3-5 条具体的事 + 为什么。复盘时可回看本批次的 decision-checkpoint Q1-Q7，区分“当时判断没跑”还是“规则本身缺口”。

例：
- 「写 PRD 时 Q1-Q7 全跑了，但 Q4 没覆盖某条现行硬规则。**规则**：把对应规则文件加入起手必查。」
- 「QA 跑了 3 次因为每次跑完忘记同步环境。**机制**：补一个可复用检查脚本或 hook。」

---

## 4. 要不要改规范？

逐条列出**具体要改哪个文件 / 哪一段**，没改的就记成"待 propose"。

| 想改的 | 改在哪 | 优先级 | 是否提 PROPOSAL |
|---|---|---|---|
| 「Q1-Q7 某项需要补前置检查」加到 decision-checkpoint | `操作系统/07_完整工作流/decision-checkpoint.md` | P1 | 是，本周提 |
| 加可复用检查脚本 | `能力资产/skills/项目体检.md` 或 `能力资产/tools/scripts/` | P2 | 是 |
| 改 ID 命名规则 | `Docs/1-需求文档/PRD-v3.md` | P3 | 暂不（先观察） |

提交流程：

1. 在 `确认改动/待审批/` 下写 PROPOSAL（一条改一份）
2. zlbdh 审批
3. 通过后按 L4 改规范（因为是规范级变更）

---

## 5. 上次复盘提的事，这次跑得怎样？

（第一次复盘可省略此节）

| 上次说要改 | 实际改了吗 | 效果 |
|---|---|---|
| 比如「sync.sh 脚本」 | ✅ 加了 | 节省了大约一半 QA 时间 |
| 比如「ID 命名」 | ❌ 没改 | 暂时没问题，继续观察 |

---

## 6. 一句话总结

让 zlbdh 在主页就能看到的一句话，类似：
> 「这批 3 个需求做下来，节奏不错；Q1-Q7 里 Q4 需要补一个可复制检查，下次试试」
